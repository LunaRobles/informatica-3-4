#include <stdio.h>

int main(){
    int base;
    printf("Enter the base number:");
    scanf("%d",&base);

    int exponent;
    printf("Enter the exponent:");
    scanf("%d",&exponent);

    int i=1;

    do {base * base;
    }
     while (i=exponent);
    
    
    printf("%d ^ %d\n", base,exponent);
     return 0;
    }