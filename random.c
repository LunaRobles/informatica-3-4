#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    srand(time(NULL));
    int i=1;
    int diceRoll;
    diceRoll= (rand() % 6) + 1; // Generates a number between 1 and 6

for {(i=1; i<6; ++i);}
do  {printf("you get a:%d\n",diceRoll);}
return 0;}

