#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    srand(time(NULL));
for{
    printf("Roll i: %d\n", diceRoll);
}
do{
    int diceRoll = (rand() % 6) + 1; // Generates a number between 1 and 6
}


    int j;
    int multi=1;
    for (j=1; j<m+1; ++j){
        multi= multi*j;
        }